# CONTEXT

이 저장소의 유비쿼터스 언어. 용어가 굳어지는 순간 여기에 기록한다.

## 용어

**로드맵 데이터** — 서비스가 유저에게 노출하는 구조화 콘텐츠(`src/data/*.json`).
노드 그래프(퀘스트·정크션·준비 목표)와 페이지 노출용 출처 크레딧으로 구성된다.
전부 자체 집필 문장이어야 하며 검증 로더를 통과해야 한다.

**출처 등록부** — 소싱에 사용한 원천 자료의 운영 기록(`sources/README.md`).
URL·수집일·상태·라이선스를 담는다. 페이지 크레딧(`src/data/sources.json`)과
역할이 다르다: 등록부는 확인용 출처까지 전부, 크레딧은 노드가 참조하는 것만.

**지식 베이스** — 에이전트가 콘텐츠를 집필·검증할 때 참조하는 내부 학습
자료(`knowledge/`, 로컬 전용·비공개). 원문 사본(raw)과 개념 단위 지식
문서(notes)로 구성된다. 유저에게 노출되지 않으며, 여기서 파생돼 로드맵
데이터로 나가는 문장은 반드시 재서술을 거친다.

**검수 큐** — 출처 간 충돌·불확실 항목의 체크리스트(`sources/REVIEW-QUEUE.md`).
운영자가 게임 지식으로 검수·서명해야 데이터 반영이 승인된다.

**기준 패치** — 어떤 사실이 유효하다고 확인된 게임 업데이트 버전.
로드맵 노드와 지식 문서 모두 이 필드를 갖는다 (노후화 판별의 축).

**월드스테이트 어댑터** — 외부 월드스테이트 API 응답을 내부 타입으로 변환하는
유일한 지점(`src/worldstate/`). 섹션(보이드 상인·낮밤 주기) 단위로 독립
실패하며 절대 던지지 않는다. 서버 측 TTL 캐시가 외부 호출을 흡수하고,
외부 장애 시 마지막 성공 스냅숏(`stale: true`) 또는 빈 스냅숏으로 폴백한다.

**문맥 위젯** — 트래커의 현재 단계(다음 목표)에 종속된 실시간 정보 표시.
독립 페이지가 아니며, 노출할 항목(관련 낮밤 주기·이번 단계 재료 획득처)은
순수 함수 `selectWidgetContext`가 결정한다. 월드스테이트를 못 받아도
로드맵 데이터에서 나오는 안내는 남고 실시간 항목만 "정보 없음"이 된다.
