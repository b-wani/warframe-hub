# CONTEXT

이 저장소의 유비쿼터스 언어. 용어가 굳어지는 순간 여기에 기록한다.

## 용어

**출처 등록부** — 소싱에 사용한 원천 자료의 운영 기록(`sources/README.md`).
URL·수집일·상태·라이선스를 담는다. 페이지 크레딧(`src/data/sources.json`)과
역할이 다르다: 등록부는 확인용 출처까지 전부, 크레딧은 노드가 참조하는 것만.

**지식 베이스** — 에이전트가 콘텐츠를 집필·검증할 때 참조하는 내부 학습
자료(`knowledge/`, 로컬 전용·비공개). 원문 사본(raw)과 개념 단위 지식
문서(notes)로 구성된다. 유저에게 노출되지 않으며, 여기서 파생돼 서비스
콘텐츠로 나가는 문장은 반드시 재서술을 거친다.

**검수 큐** — 출처 간 충돌·불확실 항목의 체크리스트(`sources/REVIEW-QUEUE.md`).
운영자가 게임 지식으로 검수·서명해야 데이터 반영이 승인된다.

**기준 패치** — 어떤 사실이 유효하다고 확인된 게임 업데이트 버전.
큐레이션 오버레이와 지식 문서 모두 이 필드를 갖는다 (노후화 판별의 축).

**월드스테이트 어댑터** — 외부 월드스테이트 API 응답을 내부 타입으로 변환하는
유일한 지점(`src/worldstate/`). 섹션(보이드 상인·낮밤 주기·보이드 균열) 단위로
독립 실패하며 절대 던지지 않는다. 목록으로 오는 섹션은 항목 단위로도 독립
실패한다 — 알아볼 수 없는 항목 하나가 그 섹션을 통째로 떨어뜨리지 않으며, "0건"과
"섹션을 못 받음"은 다른 값이다(폴백이 무엇을 메울지 가려야 한다). 서버 측 TTL 캐시가 외부 호출을 흡수하고,
외부 장애 시 마지막 성공 스냅숏(`stale: true`) 또는 빈 스냅숏으로 폴백한다.

**완료 집합** — 사용자 진행도의 유일한 원본 사실. 사용자가 완료한 것들의 id
집합이며, 원소는 성계 노드(`SolNodeNN`)와 비노드 목표가 공존한다. 이 집합만
저장하고, 그 밖의 진행 상태는 전부 여기서 파생한다.

**비노드 목표** — 성계 노드로 환원되지 않는 완료 대상(퀘스트·준비 목표).
행성 가이드 안의 체크 항목으로 노출되며, 슬러그 id로 완료 집합에 기록된다.
교차점은 여기 속하지 않는다 — 정제 노드 데이터셋의 실제 노드로 존재하므로
노드 완료로만 추적한다. 목록의 확정 재선정은 행성 가이드 저작과 함께 한다.

**노드 상태** — 성계 노드의 잠김/미클리어/클리어 3상태. 저장하지 않는다 —
완료 집합과 노드 연결 그래프(`nextNodes`)에서 계산하는 파생 상태다.

**행성 가이드** — 스타차트에서 행성을 줌인했을 때 함께 표시되는, 지식 베이스
기반의 초보자 안내 콘텐츠("이 행성에서 무엇을 해야 하는가"). 옛 로드맵의 집필
문장(`/roadmap` 제거 전 git 이력에 있다)이 이 형태로 재편된다. 비노드 목표의
체크 항목도 여기서 노출된다. 그릇은 정제 노드 데이터셋과 분리된 수동 저작
JSON(`src/data/planet-guides.json`)이며 천체 id를 키로 커밋한다 — 큐레이션
오버레이와 같은 이유다(자동 생성물에 수동 콘텐츠를 섞지 않는다). 스키마와
로더는 `src/starchart/planet-guide.ts` 하나뿐이고, 잘못된 가이드는 던지는 대신
문제 목록으로 알려 CI가 잡는다. 행성 뷰와 폴백 뷰가 같은 가이드를 읽는다 —
3D에서만 닿는 비노드 목표가 있으면 목록만 쓰는 사용자에게 그 목표는 없는 것이
된다. 프록시마를 보고 있는 동안에는 뜨지 않는다: 가이드가 말하는 것은 행성이다.

**노드 구름** — 행성 뷰에서 그 행성 주위에 뜨는 성계 노드의 그래프(노드 +
`nextNodes` 연결선 + 표시명 라벨). 좌표는 좌표 데이터셋의 노드 좌표를 배율 하나와
평행이동 하나로 옮긴 것이며, 개별 보정은 하지 않는다 — 배치를 손보는 자리는
좌표 데이터셋 하나다. 구름은 자기 천체 바로 위에 뜬 수평 원반으로, 중심에 가까운
노드가 행성 안에 파묻히지 않게 띄운다. 프록시마 노드는 여기 없다 — 행성 뷰의
⚓ 토글로 바깥 고리에 따로 붙는다.

**프록시마 고리** — 행성 뷰의 ⚓ 토글로 노드 구름 대신 뜨는, 그 행성 바깥에
선 프록시마 노드의 원반. 프록시마는 독립 천체가 아니라 해당 행성에 종속 표시
하므로 성계 뷰에는 없고, 어느 행성에 붙는지는 천체 배치표가 정한다(베일
프록시마만 붙을 행성이 없어 폴백 뷰의 항목으로만 닿는다). 배치 규칙은 노드
구름과 같다 — 좌표 데이터셋의 좌표에 배율 하나와 평행이동 하나다. 고리의 노드도
일반 노드와 똑같이 3상태·상세·완료 체크를 받으며, **행성 완료는 종속 프록시마
까지 채워졌을 때다** — 성계 뷰의 완료 아이콘이 말하는 것은 그룹이 아니라 천체다.

**연속 비행** — 성계 뷰와 행성 뷰를 오가는 유일한 전환 방식. 장면을 바꾸거나
컷을 넣지 않고, 한 장면 안에서 카메라만 보간해 움직인다(`CameraControls`).
두 뷰의 시선 방향이 같아 회전 없는 접근이 되고, 세계가 한 번도 끊기지 않는
연속성이 이 화면의 핵심 경험이다. 노드 구름이 나타나고 사라지는 것도 비행에
맡긴다 — 카메라가 멀어지는 만큼 옅어질 뿐 툭 꺼지지 않는다.

**진행도 가져오기** — 사용자가 자기 계정의 프로필 JSON을 붙여넣으면 클리어한
성계 노드를 완료 집합에 합집합으로 병합하는 조작. 완료를 지우는 일은 없으므로
언제든 재실행해도 무해하다. 반영 전에 몇 개가 새로 추가되는지 미리보기 확인을
거친다. 스틸패스 클리어 기록은 다루지 않는다 — 진행도 모델에 스틸패스 축이
없고, 스틸패스는 일반 클리어의 상위집합이라 무시해도 진행도가 줄지 않는다.

**구간 일괄 체크** — 복귀 유저가 "여기까지 완료"로 한 노드를 지정하면 그 노드와
선행 노드 전체가 한 번에 완료 처리되는 조작. 진행 엔진의 순수 함수
`completeThrough`가 선행조건을 거슬러 올라가 완료 집합을 계산하고, 완료를
해제하는 일은 없다. 스타차트에서는 노드 연결 그래프 위에서 같은 규칙으로
동작하며, "행성 전체 완료"도 이 조작의 한 형태다. 실수 방지를 위해 UI는 몇 개가 완료되는지 알리는 확인
단계를 거치며, 확인을 띄운 뒤 진행이 바뀌면 그 확인은 무효가 된다(읽은 개수와
반영되는 집합이 어긋나면 안 된다). 이미 자신과 선행이 모두 완료된 노드에는
버튼을 노출하지 않는다.

**링크 생존 점검** — 출처 등록부의 URL이 아직 살아 있는지 확인하는 운영 루틴
(`src/links/`, `pnpm check:links`). 등록부를 파싱해 URL을 뽑고(파일만 있는 행은
대상이 아니다) HEAD→GET 순으로 물어본 뒤, 죽은 링크 목록과 "등록부 갱신 필요"
건수를 담은 마크다운 리포트를 만든다. 절대 던지지 않는다 — 네트워크 오류도 결과
한 줄(dead)로 남겨 운영자가 리포트 하나로 전부 본다. 외부 사이트를 실제로
호출하므로 CI에는 넣지 않는다.

**표시명** — 게임 데이터의 언어 키(`/Lotus/Language/...`)를 공식 한국어 사전으로
해석한 사용자 노출 이름. 인게임 한국어 클라이언트와 동일 표기를 원칙으로 한다 —
행성·구역·교차점·미션명은 한국어, 노드 고유명은 영어 그대로(사전이 이미 그렇게
제공한다). 언어 분기는 없다. 사전에 없는 키는 화면에 도달하기 전에 드러나야 하며,
그래도 새면 키의 마지막 세그먼트로 폴백한다. 팩션처럼 언어 키가 아닌 enum 값만
소형 수동 매핑을 거친다. 노드에는 영문 표시명이 하나 더 인라인돼 있지만 이것은
언어 분기가 아니라 대조용 키다 — 월드스테이트가 노드를 id 없이 영문 이름으로만
알려주므로(보이드 균열 오버레이) 맞춰 볼 이름이 데이터셋 안에 있어야 한다.
사용자에게 보이는 이름은 언제나 표시명 하나다.

**그룹 유형** — 성계 노드 그룹(행성 축)의 4분류: 행성/위성(지구·달·데이모스 등
17), 프록시마(레일잭 궤도 구역 7), 특수 구역(보이드·쿠바 요새·자리만·두비리·
홀바니아 5), 릴레이(허브 노드, 1). 렌더·필터 정책은 이 유형을 축으로 정한다 —
행성/위성과 특수 구역은 지도 위 천체로, 프록시마는 해당 행성에 종속 표시,
릴레이는 지도에서 숨긴다(진행도와도 무관).

**정제 노드 데이터셋** — 게임 원본 데이터(노드 그래프)와 표시명을 빌드 파이프라인
에서 합쳐 만든, 앱이 임포트하는 유일한 스타차트 데이터 산출물. 원본 사전이나
원형 JSON은 번들에 들어가지 않는다. 갱신은 게임 패치 때 수동 스크립트 실행과
diff 검수로 이뤄지며, 기준 패치 표기를 따른다.

**좌표 데이터셋** — 스타차트 화면 배치 좌표의 유일한 원본(`src/data/starchart-layout.json`,
커밋됨). 정제 노드 데이터셋의 노드·천체마다 좌표 하나를 가지며, 노드 좌표는 자기
그룹 중심을 원점으로 한 상대 위치이고 천체 좌표는 태양을 원점으로 한 성계 뷰
위치다. 좌표는 어느 공개 소스에도 없어 수동 큐레이션 대상이다 — 생성 스크립트는
force-directed 초기 배치까지만 만들고, 보정은 이 파일을 에디터에서 직접 고친다
(편집 UI는 만들지 않는다). 재생성은 이미 있는 좌표를 보존하고 빠진 것만 채운다.
릴레이 그룹은 지도에서 숨기므로 좌표 대상이 아니다.

**천체 비주얼 표** — 성계 뷰의 천체가 어떤 텍스처·tint·대기색·크기로 보이는지
정하는 표(`src/starchart/bodies.ts`). 성계 뷰에 놓이는 22개(행성/위성 17 + 특수
구역 5)를 빠짐없이 덮어야 하고, 남거나 빠지면 CI가 잡는다 — 프록시마는 독립
천체가 아니라 해당 행성에 종속 표시하고 릴레이는 지도에서 숨기므로 둘 다 표의
대상이 아니다. 텍스처는 실사 기반이라 그대로는 워프레임 룩이 아니므로 인게임
무드를 tint와 대기색으로 얹으며, 실사 텍스처가 없는 천체는 가상 천체 텍스처를
돌려쓰고 tint로 구별한다. 좌표는 여기 없다 — 배치는 좌표 데이터셋 소관이다.

**천체 텍스처** — 성계 뷰가 쓰는 행성 이미지. 출처는 Solar System Scope
(CC BY 4.0) 하나뿐이고, 받을 목록은 텍스처 카탈로그(`src/starchart/textures.ts`)가
유일한 원본이다(`pnpm fetch:textures`). 파일은 `public/`에 커밋한다 — 배포본이
외부 사이트의 생존에 매달리지 않게 하려는 것이며, 라이선스 조건인 출처 표기는
스타차트 페이지에 노출한다. 2K 고정이다(모바일도 같은 3D를 돌린다).

**주요 노드** — 구조화 데이터만으로는 가치가 드러나지 않아 큐레이션 콘텐츠를
붙이는 성계 노드. v1 범위는 교차점(12)과 보스 노드(암살 미션 유형, 20)다.
퀘스트는 노드가 아니라 비노드 목표·행성 가이드에서 다룬다.

**큐레이션 오버레이** — 주요 노드에 얹는 수동 저작 콘텐츠 산출물. 정제 노드
데이터셋과 분리해 노드 id를 키로 커밋하며, 지식 베이스를 참조해 재서술로
저작한다. 자동 생성물인 정제 노드 데이터셋에는 수동 콘텐츠를 섞지 않는다.

**콘텐츠 기준 패치** — 페이지가 노출하는 콘텐츠 전체의 기준 패치 표기. 노드별
기준 패치를 중복 없이 모은 것이며, 보통 하나다. 여러 개가 나오면 콘텐츠가 서로
다른 패치를 기준으로 섞여 있다는 뜻이다.

**폴백 뷰** — 3D 스타차트를 렌더할 수 없거나(WebGL 불가) 쓰지 않기로 한 사용자를
위한 텍스트 기반 대체 화면(행성 아코디언 + 노드 체크리스트). WebGL 불가 환경에서는
자동으로 표시되고, 3D가 뜨는 환경에서도 수동 전환으로 접근할 수 있다. 진행도
확인·체크라는 핵심 기능은 3D 없이 여기서도 성립해야 한다. 모바일 전용 화면이
아니다 — 모바일도 기본은 데스크톱과 동일한 3D 스타차트다.

**보이드 균열 오버레이** — 실시간 월드스테이트에서 받은 활성 보이드 균열을
스타차트에 얹은 표시. 균열이 열린 노드에 로테이션 심볼이 붙고, 활성 균열 일람은
전용 목록 탭에서 읽으며 항목을 고르면 그 노드까지 연속 비행한다. 파생 표시일 뿐
저장하는 것은 없다 — 완료 집합도, 지도도, 진행도도 균열과 무관하게 온전하다.
월드스테이트를 못 받으면 이 표시만 빠진다.

**로테이션** — 보이드 균열의 렐릭 등급 6종(리스·메소·니오·액시·레퀴엠·옴니아).
지도와 목록에서 색으로 구별하되 색만으로 알리지 않는다 — 등급 이름을 글로 함께
적는다. 등급 순서가 곧 목록의 정렬 순서다.
